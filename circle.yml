deployment:
  release:
    tag: /release-.*/
    owner: datenbank-io
    commands:
      - echo "release"
  development:
    branch: development
    commands:
      - aws configure set region eu-central-1
      - ./deploy.sh